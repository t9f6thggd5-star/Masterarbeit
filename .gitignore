# --- OS / editor noise ---
.DS_Store
Thumbs.db
*.swp
*~
.vscode/
.idea/

# --- Python (scripts/build_index.py, scripts/lint.py) ---
__pycache__/
*.pyc

# --- Temporary / scratch files ---
*.tmp
*.bak
~$*

# --- Per-machine local settings (never shared/versioned) ---
# Absolute path to the external sources folder on THIS machine, read by
# scripts/lint.py to cross-check experiment IDs. See _index/README.md.
.external_sources_path

# =============================================================================
# Große Rohdaten
#
# Rohdaten (Prüfstand-Exporte, Scans, Pläne, Normen, Literatur) gehören
# grundsätzlich nicht in dieses Repository, sondern in den externen
# Quellenordner (siehe EXTERNAL_SOURCES.md) — Git-Dateigrößenlimits und
# Git LFS sind dadurch kein Thema für dieses Repository.
# =============================================================================
